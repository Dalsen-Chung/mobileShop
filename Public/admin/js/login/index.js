layui.use('form', function () { 
    var form = layui.form;
    // form.on('submit(loginForm)', function(data){
    //     layer.msg(JSON.stringify(data.field));
    //     return false;
    // });
});